import React from 'react'
import ReactDOM from 'react-dom/client'
import GitPulse from './GitPulse.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GitPulse />
  </React.StrictMode>
)
